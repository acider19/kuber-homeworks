apiVersion: v1
kind: Pod
metadata:
  name: netology-web
  labels:
    app: netology-web
spec:
  containers:
    - name: echoserver
      image: ealen/echo-server:latest
      env:
        - name: PORT
          value: "8080"
      ports:
        - containerPort: 8080
