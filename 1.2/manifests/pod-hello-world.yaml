apiVersion: v1
kind: Pod
metadata:
  name: hello-world
  labels:
    app: hello-world
spec:
  containers:
    - name: echoserver
      image: ealen/echo-server:latest
      env:
        - name: PORT
          value: "8080"
      ports:
        - containerPort: 8080
